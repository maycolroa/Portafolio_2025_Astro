---
export interface Props {
  items: Array<{
    period: string;
    title: string;
    company?: string;
    points: string[];
  }>;
}

const { items } = Astro.props;
---

<div class="relative pl-8 mt-10 space-y-12">
  <!-- Línea vertical -->
  <div class="absolute left-0 top-0 h-full w-0.5 bg-emerald-400 bg-opacity-30"></div>
  
  {items.map((item, index) => (
    <div class="relative">
      <!-- Punto en la línea temporal -->
      <div class="absolute left-[-8px] top-2 w-3 h-3 rounded-full bg-emerald-400 border-2 border-black z-10"></div>
      
      <!-- Periodo -->
      <div class="font-mono font-bold mb-3 text-emerald-400">{item.period}</div>
      
      <!-- Contenido -->
      <div class="bg-gray-900 rounded-lg p-6 transition-all duration-300 hover:-translate-y-1 hover:shadow-lg">
        <h3 class="text-xl font-bold mb-3">{item.title}</h3>
        
        <ul class="space-y-2">
          {item.company && (
            <li class="flex items-start">
              <span class="text-emerald-400 mr-2">•</span>
              <span>{item.company}</span>
            </li>
          )}
          
          {item.points.map(point => (
            <li class="flex items-start">
              <span class="text-emerald-400 mr-2">•</span>
              <span class="text-gray-400">{point}</span>
            </li>
          ))}
        </ul>
      </div>
    </div>
  ))}
</div>
