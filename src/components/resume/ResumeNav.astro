---
export interface Props {
  activeSection: string;
}

const { activeSection = 'experience' } = Astro.props;
---

<div class="space-y-4">
  <h2 class="text-3xl font-bold mb-4">Why hire me?</h2>
  <p class="text-gray-400 mb-8">
    Desarrollador apasionado con experiencia en creación de soluciones web modernas y responsivas.
    Enfocado en resultados y en brindar la mejor experiencia al usuario.
  </p>
  
  <div class="flex flex-col md:flex-row flex-wrap gap-2">
    <a 
      href="#experience" 
      data-section="experience"
      class={`resume-nav-item px-5 py-3 rounded-lg font-medium transition-colors duration-300 text-center ${
        activeSection === 'experience' 
        ? 'bg-emerald-400 text-[#000000]' 
        : 'bg-gray-900 text-[#FFFFFF] hover:bg-opacity-30 hover:text-emerald-400'
      }`}
    >
      Experience
    </a>
    <a 
      href="#education" 
      data-section="education"
      class={`resume-nav-item px-5 py-3 rounded-lg font-medium transition-colors duration-300 text-center ${
        activeSection === 'education' 
        ? 'bg-emerald-400 text-[#000000]' 
        : 'bg-gray-900 text-[#FFFFFF] hover:bg-opacity-30 hover:text-emerald-400'
      }`}
    >
      Education
    </a>
    <a 
      href="#skills" 
      data-section="skills"
      class={`resume-nav-item px-5 py-3 rounded-lg font-medium transition-colors duration-300 text-center ${
        activeSection === 'skills' 
        ? 'bg-emerald-400 text-[#000000]' 
        : 'bg-gray-900 text-[#FFFFFF] hover:bg-opacity-30 hover:text-emerald-400'
      }`}
    >
      Skills
    </a>
    <a 
      href="#about" 
      data-section="about"
      class={`resume-nav-item px-5 py-3 rounded-lg font-medium transition-colors duration-300 text-center ${
        activeSection === 'about' 
        ? 'bg-emerald-400 text-[#000000]' 
        : 'bg-gray-900 text-[#FFFFFF] hover:bg-opacity-30 hover:text-emerald-400'
      }`}
    >
      About me
    </a>
  </div>
</div>
